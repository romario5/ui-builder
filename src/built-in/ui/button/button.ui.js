UI.register({
    name: 'built-in / Button',

    scheme: {
        wrap: '@button [type = button]'
    },

    params: {
        disabled: false,
        color: 'primary'
    },

    onRender(inst, params) {
        // Apply parameters.
        // TODO Add button parameters applying.

        // Handle localization.
        // TODO Add button localization handling.

        // Attach click handler.
        // TODO Add button handlers.
    },

    styles: {
        wrap: {

        }
    }
});